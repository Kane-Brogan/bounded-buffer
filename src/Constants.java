package kanebrogan.boundedbuffer.src;

/*
 * A class that contains the constant variables used by 
 * the bounded-buffer and the associated processes.
 * 
 * @author Kane Brogan 
 */
public class Constants {
    public static final int MAXSLEEP = 1000, MAXRAND = Integer.MAX_VALUE;

    // run time is set to 10 seconds for all tests
    public static final long SLEEPTIME = 10000;
}